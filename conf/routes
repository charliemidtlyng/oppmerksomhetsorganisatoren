# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                         controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file             controllers.Assets.at(path="/public", file)

## Tasks
GET     /adresser                 controllers.Application.adresser
POST    /adresser                 controllers.Application.nyAdresse
POST    /adresser/:id/slett       controllers.Application.slettAdresse(id: Long)
#
GET     /personer                 controllers.Application.personer
POST    /personer                 controllers.Application.nyPerson
POST    /personer/:id/slett       controllers.Application.slettPerson(id: Long)